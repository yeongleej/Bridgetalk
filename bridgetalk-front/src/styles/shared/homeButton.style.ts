import styled from 'styled-components';

export const Wrapper = styled.button`
  position: fixed;
  top: 3svh;
  left: 2svw;

  background-color: transparent;

  border: none;

  img {
    width: 5.8svw;
  }
`;
